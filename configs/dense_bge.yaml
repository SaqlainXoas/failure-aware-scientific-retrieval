# Dense first-stage retrieval via BAAI/bge-small-en-v1.5 (model-card query instruction,
# normalized embeddings, exact cosine similarity — no FAISS).
pipeline: dense_bge
dataset: beir/scifact
seed: 42
splits_dir: splits
device: auto
candidate_depth: 100
model_name: BAAI/bge-small-en-v1.5
model_revision: 5c38ec7c405ec4b44b94cc5a9bb96e735b38267a
query_instruction: "Represent this sentence for searching relevant passages: "
batch_size: 32
